@model HomeIndexViewModel
@{
  ViewBag.Title = "Index";
}

<partial name="_Header" Model="@Model.SearchVM">

<h2 class="text-center">Recently Uploaded</h2>

<div class="container-fluid">
  <partial name="_GalleryWithPagination" model="@Model.Photos">
</div>

